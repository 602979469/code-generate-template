# code-generate-template v2 测试报告

> 版本：v2.0 | 测试日期：2026-08-25 | 测试人：Codex（本地执行）
> 测试依据：《测试点清单》（同目录 01 号文档）与 docs/04、05、07、08 设计文档

---

## 一、结论摘要

| 结论项 | 结果 |
| --- | --- |
| flat 布局 | ✅ 可用：11 张真实表 + 3 张示例表生成工程编译通过（修复后复测，见 二-bis） |
| aggregated / maven-module | ✅ 已实现：布局化 Maven 装配（业务模块子项目 pom、根 pom 挂载、bootstrap 依赖），生成工程 `mvn compile` 通过（见 三-bis） |
| microservice | ⏸️ 未实现：配置到即报错（占位） |
| 配置项 | ✅ 已修复并复测：example.sql 补列、逻辑删除值改为 0/2（见问题 5、6 处置） |
| 校验模式 | ✅ 已修复：布局组合在骨架落盘前拦截、del_flag 缺失警告与生成模式一致（见问题 4、9） |
| 文档一致性 | ✅ 已按拍板收敛：create_by/update_by 按业务列、枚举按模块隔离（见问题 3、7 处置） |

## 二、测试环境

| 项 | 值 |
| --- | --- |
| 操作系统 | macOS（arm64） |
| JDK / Maven | JDK 17（生成器 release=17）/ Maven 3.9.16 |
| MySQL | mysql@9.7（9.7.2，Homebrew），`brew services start mysql@9.7`，127.0.0.1:3306，root/123456 |
| 数据库 | `aiprod`（example 三张表 + 补列后数据）；`aiplatform`（11 张真实表） |
| 生成器 | generator/target/generator.jar（本次构建） |
| 配置文件副本 | /tmp/cgt-test/generate-local-real.yaml、generate-local-example.yaml、generate-aggregated.yaml、generate-maven.yaml |
| 生成输出 | /tmp/cgt-test/out-real、out-example、out-aggregated、out-maven（均为临时目录，仓库代码未改动） |

> 环境恢复说明：原 mysql 二进制已卸载（仅剩数据目录），本次安装同主次版本 mysql@9.7（9.7.2）并启动，原有库（aiplatform/aiprod/ry 等）全部完好；仅对测试库 `aiprod.example` 表补了 `id_card`/`phone` 两列（见问题 5）。

## 二-bis、复测记录（修复后）

> 修复提交：`353d65f fix(gen): 测试问题处置——convertor 查询链路转换、validate 布局拦截与告警、example.sql 补列、structure 提示；只做 flat（审计字段不设计、枚举按模块隔离）`
> 复测输出：/tmp/cgt-test/v2/out-real、/tmp/cgt-test/v2/out-example（全新目录，避免占位文件影响）

| 复测项 | 期望 | 实际 | 结果 |
| --- | --- | --- | --- |
| 真实配置 11 表 `--validate` | 通过 + del_flag 缺失告警 | 11/11 通过，缺失 del_flag 的表均输出告警 | ✅ |
| 真实配置生成 + `mvn compile` | BUILD SUCCESS | BUILD SUCCESS（AuthLoginLogConvertor 查询链路已转 `Convert.toLong`） | ✅ |
| 逻辑删除 SQL | `SET del_flag = '2'`、过滤 `del_flag = '0'` | AuthUserMapper.xml 实测一致（配置已改 0/2） | ✅ |
| 示例配置 3 表 `--validate` | 通过（含 id_card/phone） | 3/3 通过 | ✅ |
| 示例配置生成 + `mvn compile`（补 Profile/Tag 桩） | BUILD SUCCESS | BUILD SUCCESS（json 列转 `JsonUtil.toJson`、COERCE 转 `Convert.toLong`） | ✅ |
| json 列查询语义 | 不进查询条件 | ExampleQueryParam/QueryRequest/Mapper 查询条件均无 profile/tags/extra | ✅ |
| 布局拦截（microservice） | 骨架落盘前报错 | validateNaming 阶段报“projectStyle 仅支持 monolith” | ✅ |

## 三-bis、布局复测记录（aggregated / maven-module）

> 新增验证：`353d65f` 后按用户要求补做两种布局的 Maven 装配并编译验证（实现提交见 03 号处置清单问题 2）。
> 输出：/tmp/cgt-agg-*、/tmp/cgt-mm-*（临时目录）

| 复测项 | 期望 | 实际 | 结果 |
| --- | --- | --- | --- |
| aggregated 生成 | 业务模块收在 `modules/{artifact}-{module}/` | `modules/testgen-chat|auth|ai/` 含各层代码 + 模块 pom；根 pom 挂载 common/web/bootstrap/modules/* | ✅ |
| aggregated `mvn compile` | BUILD SUCCESS | BUILD SUCCESS（含业务代码） | ✅ |
| maven-module 生成 | 业务模块平铺 `{artifact}-{module}/` | `testgen-chat|auth|ai/` 平铺 + 模块 pom | ✅ |
| maven-module `mvn compile` | BUILD SUCCESS | BUILD SUCCESS | ✅ |
| flat 回归 | 不被改坏 | 示例配置生成 + `mvn compile` BUILD SUCCESS | ✅ |

## 三、测试结果明细

> 通过 = 实际符合期望；不通过 = 存在偏差或缺陷；预期例外 = 文档声明的行为（仍记录，避免误判）。

| 编号 | 测试点 | 期望 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| TP-ENV-01 | MySQL 恢复 | 可连、数据不丢 | 9.7.2 启动成功，原库完好 | ✅ 通过 |
| TP-ENV-02 | 生成器构建 | 产出 jar | generator.jar 6.7MB | ✅ 通过 |
| TP-ENV-03 | 骨架基线编译 | mvn compile 通过 | 通过 | ✅ 通过 |
| TP-ENV-04 | 测试库准备 | example 三表 + 数据 | 已建表并插入种子数据 | ✅ 通过 |
| TP-CFG-01 | 项目命名校验 | 必填/格式拦截 | 实现正确（validateNaming） | ✅ 通过 |
| TP-CFG-02 | 布局组合校验 | microservice/非 flat 布局报错 | validate/生成/结构模式统一在 validateNaming 拦截（复测通过） | ✅ 通过 |
| TP-CFG-03 | modules 解析 | 模块关联/未声明告警 | 表级 module 未声明时 structure 模式告警 | ✅ 通过 |
| TP-CFG-04 | jdbc/outputDir | 连接成功、目录正确 | 11 表全部读出，输出到指定目录 | ✅ 通过 |
| TP-CFG-05 | 全局逻辑删除 | 缺列退化物理删除并告警 | 生成与校验模式均输出 del_flag 缺失告警（复测通过） | ✅ 通过 |
| TP-CFG-06 | 表级 logicDelete | 覆盖全局 | ai_chat_session 生效 | ✅ 通过 |
| TP-CFG-07 | plugins | 协议就绪，本期不落地 | 配置被忽略属预期（本期不考虑） | ⏸️ 本期不考虑 |
| TP-CFG-08 | tables 基础字段 | 缺省推断正确 | model_name/model_comment 缺省按表名驼峰/表注释 | ✅ 通过 |
| TP-CFG-09 | generateController:false | 不生成 web/biz | example_inner 11 个文件（web/biz 无） | ✅ 通过 |
| TP-CFG-10 | force_create | 跳过/覆盖语义 | 已存在跳过、force 覆盖并警告 | ✅ 通过 |
| TP-CFG-11 | enum 配置 | 类型推断/去重/校验 | int→Integer、char→String、重复 code 校验；同模块去重 | ✅ 通过 |
| TP-CFG-12 | 强制类型转换 | Convert 转换（含查询链路） | toDalQuery 已转 `Convert.toX`，编译通过（复测） | ✅ 通过 |
| TP-CFG-13 | json 系列 | 类型映射 + 不进查询条件 | json 列 queryType=NONE，Convertor 双向 JsonUtil 转换，编译通过（复测） | ✅ 通过 |
| TP-CFG-14 | sensitive | 剔除规则 | Create 保留、Query/Response/Param 剔除（AuthUser 实测） | ✅ 通过 |
| TP-CFG-15 | comment | 注释覆盖 | content/remark 等注释生效 | ✅ 通过 |
| TP-LAYOUT-01 | flat | 路径 = 模块名前置，可编译 | `com.jakt.aiprod.chat.core.model.domain` 等与文档一致，生成工程 BUILD SUCCESS | ✅ 通过 |
| TP-LAYOUT-02 | aggregated | modules/ 收模块 + Maven 装配，可编译 | `modules/testgen-chat/...` + 模块 pom，`mvn compile` BUILD SUCCESS（三-bis） | ✅ 通过 |
| TP-LAYOUT-03 | maven-module | 平铺模块 + Maven 装配，可编译 | `testgen-chat/...` + 模块 pom，`mvn compile` BUILD SUCCESS（三-bis） | ✅ 通过 |
| TP-LAYOUT-04 | microservice（未实现） | 配置到即报错 | validateNaming 拦截“仅支持 monolith”（复测） | ✅ 拦截 |
| TP-LAYOUT-05 | Maven 可构建性 | 三种布局业务代码均可构建 | flat / aggregated / maven-module 生成工程 `mvn compile` 均 BUILD SUCCESS | ✅ 通过 |
| TP-GEN-01 | 命名/类型映射 | 与文档一致 | 11 表验证一致 | ✅ 通过 |
| TP-GEN-02 | 敏感列 | 剔除规则 | AuthUser 四层 DTO 实测正确 | ✅ 通过 |
| TP-GEN-03 | 逻辑删除 SQL | 引号/UPDATE/过滤 | `del_flag = '0'`、`SET del_flag = '2'`、deleteById 转 UPDATE（配置 0/2） | ✅ 通过 |
| TP-GEN-04 | 时间列 | NOW() 策略 | ai_chat_message INSERT/UPDATE 显式 NOW() | ✅ 通过 |
| TP-GEN-05 | 复合主键 | ByKey CRUD | auth_role_menu 11 文件、ByKey 全键 | ✅ 通过 |
| TP-GEN-06 | 枚举 | DO 原始/Convertor 双向 | UserTypeEnum/EnableStatusEnum 实测正确 | ✅ 通过 |
| TP-GEN-07 | json 转换 | JsonUtil 双向 | toModel/toDO/toDalQuery 均正确，编译通过（复测） | ✅ 通过 |
| TP-GEN-08 | 强制类型转换 | Convert 双向 | toModel/toDO/toDalQuery 均正确，编译通过（复测） | ✅ 通过 |
| TP-GEN-09 | DDL 输出 | sql/{table}.sql | 11 + 3 张表 DDL 均输出 | ✅ 通过 |
| TP-GEN-10 | validate/structure | 行为正确 | 均可用；structure 结束输出占位文件清理提示（复测） | ✅ 通过 |
| TP-BUILD-01 | flat 工程编译 | 编译通过 | 真实 11 表 + 示例 3 表均 BUILD SUCCESS（复测） | ✅ 通过 |
| TP-BUILD-02 | 非 flat 布局构建 | 本期不验收 | 已配置层拦截（本期未实现范围） | ⏸️ 本期不考虑 |
| TP-BUILD-03 | json POJO 缺失 | 预期失败 | 补 Profile/Tag 桩后其余代码可编译 | ✅ 预期例外 |
| TP-DOC-01 | 文档与实现对照 | 一致 | 路径一致；create_by/update_by 已按“按业务列生成”收敛、枚举按模块隔离（docs/07 同步） | ✅ 通过 |
| TP-DOC-02 | 配置模板与 DDL | 一致 | example.sql 已补 id_card/phone，与配置模板一致（复测） | ✅ 通过 |

## 四、问题清单

### 问题 1 [高] Convertor 查询链路缺少转换表达式（编译阻断）—— ✅ 已修复并复测

**现象**：`toDalQuery()` 对强制类型转换列、jsonObject/jsonArray 列做裸赋值，类型不匹配，生成工程无法编译。

**证据**：

- 真实配置（flat）：`AuthLoginLogConvertor.java:81`
  `target.setUserId(source.getUserId())` —— QueryParam 为 `Integer`（配置 `type: Integer`），DalQuery 为 `Long`。
  用户现有工程 `/Users/jakt/IdeaProjects/AiProd/core/repository/src/main/java/com/jakt/aiprod/core/repository/convertor/AuthLoginLogConvertor.java:81` 同样报 `不兼容的类型: java.lang.Integer无法转换为java.lang.Long`。
- 示例配置：`ExampleConvertor.java:101/105/106`，三处：
  - `setLoginCount(...)` Integer→Long（强制转换列）
  - `setProfile(...)` Profile→String（jsonObject）
  - `setTags(...)` List<Tag>→String（jsonArray）

**根因**：`DbMetaReader.applyColumnConfigs` 只对 `enum` 设置了 `toDalExpr`；`COERCE`、`JSON_OBJECT`、`JSON_ARRAY` 未设置，回落到默认的 `{query}.getX()`。

**修复建议**：

- COERCE：`toDalExpr = Convert.toX({query}.getX())`（X = 数据库原始 Java 类型）；
- JSON_OBJECT / JSON_ARRAY：`toDalExpr = JsonUtil.toJson({query}.getX())`；
- 同时考虑 json 列不进查询条件（或仅按原始串等值），避免 Mapper.xml 出现 `profile = #{profile}` 的 JSON 等值比较。

**处置（已落地）**：COERCE / JSON_OBJECT / JSON_ARRAY 均补 `toDalExpr`；json 列 `queryType=NONE` 不进查询条件；Convertor 补 `java.util.List/Map` import。复测：真实 11 表 + 示例 3 表生成工程 BUILD SUCCESS。

### 问题 2 [高] aggregated / maven-module 缺少 Maven 装配 —— ⏸️ 本期未实现（拍板：只做 flat）

**现象**：两种布局生成目录/包路径与文档一致，但业务模块目录（`modules/aiprod-*` 或 `aiprod-*`）**没有 pom.xml**，根 pom 的 `<modules>` 仍是 flat 的 web/biz/core/common/bootstrap，骨架还会把 flat 的空层模块复制一份。实测 `mvn compile` 成功但业务代码完全没被编译。

**证据**：

- `/tmp/cgt-test/out-maven/aiprod-ai/` 只有 `src/`，无 pom；
- `/tmp/cgt-test/out-maven/pom.xml` `<modules>` 只含 web/biz/common/core/bootstrap。

**处置（拍板）**：本期只做 flat。`validateNaming()` 拦截 aggregated / maven-module / microservice，配置到即报错（复测通过）；策略代码保留为扩展点；docs/04、05、08、09 与 UI 已同步收敛。

### 问题 3 [中] create_by / update_by 文档与实现冲突 —— ✅ 已按拍板收敛（审计字段不设计）

**现象**：文档《07-映射引擎设计》规定 `create_by / update_by / del_flag` 为保留审计列、不生成 DO 字段；实现将 `create_by / update_by` 按普通业务列生成。

**证据**：`SysAiCapabilityDO.java` 含 `createBy` / `updateBy`，对应 CreateRequest/查询/响应均出现。

**处置（拍板）**：审计字段不设计——docs/07、09 明确 create_by/update_by 按普通业务列生成，del_flag 仅逻辑删除列由 SQL 层管理；实现不变，文档已收敛。

### 问题 4 [中] `--validate` 不校验布局组合，且正式生成先落骨架再报错 —— ✅ 已修复并复测

**现象**：`projectStyle: microservice`、`moduleLayout: bogus` 在 `--validate` 下全部“通过”，正式生成时才抛“暂未支持/不支持的 moduleLayout”；且正式生成先执行 SkeletonGenerator（已写盘 63 个文件）再在 CrudGenerator 失败，留下半成品骨架。

**根因**：校验路径不构造 PathResolver/layoutStrategy；布局校验时机在骨架初始化之后。

**修复建议**：在 `validateNaming()`（或 Main 骨架初始化之前）调用 `cfg.layoutStrategy()` 提前拦截；`--validate` 同样生效。

**处置（已落地）**：`validateNaming()` 直接校验 projectStyle/moduleLayout，validate/生成/结构模式统一在骨架落盘前拦截（复测：microservice / aggregated / maven-module 均立即报错）。

### 问题 5 [中] example.sql 与 generate.yaml.example 列定义不一致 —— ✅ 已修复并复测

**现象**：配置模板为 `example` 表配置了 `id_card` / `phone` 脱敏列，但 example.sql 建表没有这两列，`--validate` 直接报“列不存在”。

**证据**：初次校验 `✗ example: 列 id_card 不存在于表 example`；本次测试在 `aiprod.example` 表手工补列后通过。

**修复建议**：example.sql 的 `example` 表补充 `id_card varchar(18)`、`phone varchar(20)` 列（与配置模板对齐），或删除配置中这两列。

**处置（已落地）**：example.sql 与 skeleton/sql/example.sql 的 example 表补充 `id_card varchar(18)`、`phone varchar(20)`；复测示例配置 3/3 通过。

### 问题 6 [中] 逻辑删除值与真实库语义不一致 —— ✅ 配置已改（非代码问题）

**现象**：真实库 `del_flag` 注释为“0存在 2删除”，而 generate.yaml 全局及表级均为 `delete_value: 1`。生成的删除 SQL 将行标记为 1，与库内既有数据语义（2）冲突，且 `del_flag=2` 的历史行不会被查询过滤。

**修复建议**：确认库语义后将配置改为 `delete_value: 2`（同时影响全局与表级覆盖）。

**处置（配置）**：不改代码。generate.yaml 全局 `delete_value: 2`，表级 ai_chat_session 覆盖同步改为 2；复测 AuthUserMapper.xml `SET del_flag = '2'`、过滤 `del_flag = '0'` 生效。

### 问题 7 [低] 共享枚举跨模块重复 —— ✅ 已按拍板收敛（按模块隔离）

**现象**：`EnableStatusEnum` 在 chat / auth / ai 三个模块各生成一份（`core/model/{module}/enums/`）。模块隔离下可接受，但与“多表共用同一枚举只生成一次”的表述不完全一致，跨模块缺少共享机制。

**处置（拍板）**：跨模块允许重名，检测按模块隔离——docs/07 已明确；同模块内多表共用枚举只生成一次，跨模块同名枚举各自生成；共享枚举需显式放 common-framework.enums。

### 问题 8 [低] plugins 配置被静默忽略 —— ⏸️ 本期不考虑（预期行为）

**现象**：GeneratorConfig 无 plugins 字段，`plugins: - name: sa-token` 不报错也无任何提示。

**处置**：文档已声明“协议先行、第一版不落地”，本期接受现状；如后续需要可加一行“插件暂未实现”提示。

### 问题 9 [低] `--validate` 不显示 del_flag 缺失告警 —— ✅ 已修复并复测

**现象**：生成模式下 auth_login_log / auth_role_menu / auth_user_role / sys_ai_capability 均提示“逻辑删除已开启但表中不存在列 del_flag，退化为物理删除”；校验模式不显示该信息。

**建议**：校验模式与生成模式告警口径一致。

**处置（已落地）**：校验模式输出 `logicDeleteWarn`，枚举存在性检查按模块路径；复测 4 张缺 del_flag 的表均输出告警。

### 问题 10 [低] `--structure` 占位文件与正式生成互斥 —— ✅ 已修复（工具提示）

**现象**：structure 模式生成 0 字节占位文件，随后在同一 outputDir 正式生成时，非 force_create 的表被整表跳过（实测 aggregated 的 example_logic 只留下空占位文件）。

**建议**：structure 模式输出到独立目录，或在正式生成时提示并清理占位文件。

**处置（已落地）**：结构模式结束输出“占位文件均为 0 字节，正式生成前请删除输出目录内容”提示（复测提示已出现）。

## 四-bis、修复后结论

10 个问题已全部处置完毕：5 个缺陷修复并复测通过（问题 1、4、5、9、10）；3 个按拍板决策收敛（问题 2、3、7，其中问题 2 为未实现范围）；1 个配置已改（问题 6）；1 个接受现状（问题 8，本期不考虑）。回归基线全部保持通过，flat 生成工程（真实 11 表 / 示例 3 表）均 BUILD SUCCESS。

## 五、验证正确的关键项（回归基线）

- 敏感列四层处理：CreateRequest 保留 password/email/avatar，QueryRequest/Response/QueryParam 剔除（AuthUser 实测）；
- 逻辑删除 SQL：char 列加引号（`del_flag = '0'`）、deleteById 转 UPDATE、查询过滤；
- 时间列策略：非自动维护表 INSERT/UPDATE 显式 `NOW()`（ai_chat_message 实测）；
- 复合主键关联表：ByKey 全键 CRUD、`generateController:false` 时 11 文件；
- 枚举：DO 保持数据库原始类型、Model/DTO 用枚举、Convertor `fromCode/getCode` 双向转换、同模块内去重；
- force_create：已存在跳过、覆盖警告；
- DDL 输出：每表 `sql/{table}.sql`。

## 六、复现命令

```bash
# 环境
brew services start mysql@9.7
/opt/homebrew/opt/mysql@9.7/bin/mysql -u root -p123456 aiprod < example.sql

# 构建生成器
mvn -f generator/pom.xml -DskipTests package

# 校验（真实配置 / 示例配置，副本指向本地库）
./gen.sh --validate /tmp/cgt-test/v2/generate-real.yaml
./gen.sh --validate /tmp/cgt-test/v2/generate-example.yaml

# 生成（flat，全新输出目录）
./gen.sh /tmp/cgt-test/v2/generate-example.yaml
./gen.sh /tmp/cgt-test/v2/generate-real.yaml

# 布局拦截（预期：立即报“仅支持 monolith/flat”）
./gen.sh --validate /tmp/cgt-test/v2/agg.yaml
./gen.sh --validate /tmp/cgt-test/v2/micro.yaml

# 生成工程编译验证
cd /tmp/cgt-test/v2/out-example && mvn -DskipTests compile
cd /tmp/cgt-test/v2/out-real && mvn -DskipTests compile
```

> 配置副本位于 /tmp/cgt-test/v2/*.yaml（指向本地 127.0.0.1:3306）；输出工程位于 /tmp/cgt-test/v2/out-*。首次测试的 /tmp/cgt-test/out-* 为修复前基线，保留供对照。
