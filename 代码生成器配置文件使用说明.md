# 代码生成器配置文件使用说明

> 适用对象：code-generate-template 生成器的 `generate.yaml` 配置文件。
> 生成器完全由 YAML 配置驱动，不使用命令行参数；本说明覆盖全部配置项及生成行为。
> 配置模板见 [generate.yaml.example](generate.yaml.example)，示例建表脚本见 `skeleton/sql/example.sql`。

## 1. 配置文件的作用

一次 `./gen.sh <配置文件>` 调用完成两件事：

1. **项目骨架初始化**：复制 `skeleton/` 样板并按项目命名规则改名，产出可编译的多模块 Spring Boot 工程；
2. **表级 CRUD 生成**：连接 MySQL 读取表结构，按 `tables` 配置渲染模板，输出分层完整的 CRUD 代码与建表 SQL。

## 2. 快速开始

前置要求：JDK 17+、Maven 3.9+、可连接的 MySQL（首次运行 `gen.sh` 会自动构建生成器）。

```bash
# 1) 生成配置模板（当前目录 generate.yaml + example.sql）
./gen.sh

# 2) 建示例表（可选：只跑自己的业务表则跳过）
mysql -uroot -p < ./example.sql

# 3) 编辑 generate.yaml：项目命名 / jdbc / outputDir / tables
# 4) 按配置生成
./gen.sh ./generate.yaml
```

## 3. 配置结构总览

```yaml
# ---------------- 项目命名（全部必填，无默认值） ----------------
projectPrefix: AiProd          # 项目前缀，用于启动类名 AiProdApplication
toolPrefix: AiProd             # 工具类/异常/常量前缀
groupId: com.jakt              # Maven groupId
projectArtifactPrefix: aiprod  # artifactId/包名后缀（小写）

# ---------------- 数据库（配置了 tables 时必填） ----------------
jdbc:
  url: jdbc:mysql://localhost:3306/aiprod?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: "123456"

# 生成输出根目录（骨架与表级 CRUD 的落点）
outputDir: /Users/jakt/IdeaProjects/AiProd

# ---------------- 全局逻辑删除（可选，默认不开启） ----------------
globalLogicDelete:
  enable: false
  column_name: del_flag
  normal_value: 0
  delete_value: 1

# ---------------- 业务表 ----------------
tables:
  - db_table_name: sys_dept
    # model_name / model_comment / force_create / generateController / logicDelete / columns 均可选
```

## 4. 项目命名

| 配置项 | 必填 | 约束 | 示例 |
| --- | --- | --- | --- |
| `projectPrefix` | 是 | 驼峰字母/数字 | `AiProd` |
| `toolPrefix` | 是 | 驼峰字母/数字 | `AiProd` |
| `groupId` | 是 | Maven groupId | `com.jakt` |
| `projectArtifactPrefix` | 是 | 小写字母开头，后跟小写字母/数字 | `aiprod` |

命名作用：

- `projectPrefix` → 启动类名 `${projectPrefix}Application`；
- `toolPrefix` → 工具类/异常/常量前缀（`AiProdException`、`AiProdInvoker`、`AiProdTemplate` 等），保证互相引用一致；
- `groupId` + `projectArtifactPrefix` → 基础包名 `com.jakt.aiprod`，同时决定 artifactId。

`groupId` 决定基础包名前半段：点号转斜杠后就是生成工程里的物理包目录（如 `com.jakt` → `com/example`），等价于包目录前缀；它与 `outputDir`（工程根目录的落点）是两回事，见下一节。

校验失败（缺失、含连字符、大写 artifactId 等）会在生成前直接报错退出，不产生任何输出。

## 5. 数据库与输出目录

```yaml
jdbc:
  url: jdbc:mysql://localhost:3306/aiprod?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: "123456"

outputDir: /Users/jakt/IdeaProjects/AiProd
```

- `jdbc`：配置了 `tables` 时 `url` / `username` 必填；用于读取 `information_schema` 表结构与 `SHOW CREATE TABLE`；
- `outputDir`：生成项目落点，不存在时自动创建；缺省为当前目录；
- 不配置 `tables` 时只做骨架初始化，不需要 `jdbc`。

> `outputDir` 是整个工程放在哪个根目录；`groupId` 决定的是 `src/main/java` 下的包目录层级（`com/example/...`）。两者相互独立。

## 6. 逻辑删除（可选）

两级配置，**表级优先于全局**：

```yaml
# 全局（顶层）
globalLogicDelete:
  enable: true
  column_name: del_flag
  normal_value: 0
  delete_value: 1

# 表级（tables[].logicDelete，属性同全局，覆盖全局）
logicDelete:
  enable: true
  column_name: del_flag
  normal_value: 0
  delete_value: 1
```

规则：

- `enable: true` 时 `column_name` / `normal_value` / `delete_value` 三项必填，缺失在生成前报错；
- 配置生效且 `column_name` 在表中真实存在时才启用逻辑删除；未配置 / `enable: false` / 列不存在 → 物理删除；
- `del_flag` 属于保留审计列，不生成 DO 字段，由 SQL 层统一管理。

行为（以 `del_flag`、正常值 0、删除值 1 为例）：

| 操作 | 物理删除 | 逻辑删除 |
| --- | --- | --- |
| `selectById` | `WHERE id = ?` | `WHERE id = ? AND del_flag = 0` |
| `selectPage` / `selectList` / `countByQuery` | 原样 | 追加 `AND del_flag = 0` |
| `deleteById` | `DELETE FROM t WHERE id = ?` | `UPDATE t SET del_flag = 1 WHERE id = ? AND del_flag = 0` |
| `update` / `updateByCondition` | `WHERE id = ?` | `WHERE id = ? AND del_flag = 0` |
| `insert` | 原样 | 原样（`del_flag` 由数据库默认值写入） |

## 7. 业务表配置 tables

### 7.1 基本项

| 配置项 | 必填 | 说明 |
| --- | --- | --- |
| `db_table_name` | 是 | 数据库表名 |
| `model_name` | 否 | Java 类名；缺省 = 表名驼峰（`example_logic` → `ExampleLogic`） |
| `model_comment` | 否 | 中文实体名，用于所有 javadoc/日志注释拼接；缺省 = 数据库表注释，表注释为空时回退表名 |

### 7.2 生成控制

```yaml
# force_create 只有表级配置，没有全局配置（全局开关目前仅 globalLogicDelete）
tables:
  - db_table_name: example_inner
    model_name: ExampleInner
    model_comment: 内部示例
    generateController: false   # 内部表：不生成 web 层 7 个文件

  - db_table_name: example
    model_name: Example
    model_comment: 示例
    force_create: true          # 强制覆盖该表已存在文件（危险，会覆盖手动修改的代码）
```

- `force_create`（默认 `false`）：表已存在（DO 或同名枚举）时默认**整表跳过**（防覆盖）；`true` 时覆盖该表所有文件并输出警告（危险，会覆盖手动修改的代码）；
- `force_create` 仅表级、无全局配置：覆盖操作是"针对某张表"的语义，逐表声明更安全，不会误伤其他表；若将来需要全局默认值，建议命名为 `globalForceCreate`，与表级 `force_create` 区分（现状不需要）；
- `generateController`（默认 `true`）：`false` = 内部表，只生成 10 个数据/服务层文件（DO / Mapper / Mapper.xml / Model / QueryParam / Repository / RepositoryImpl / Convertor / Service / ServiceImpl），不生成 web 层 7 个文件（Controller、ParamChecker、4 个 DTO、Assembler）与 biz 层 Manager/ManagerImpl（内部表无对外接口，Manager 无存在必要）；
- 每张表独立判定成功 / 跳过 / 强制覆盖，互不影响。

### 7.3 columns 列级配置（重点）

列级配置让 Model 层展示更有表达力的类型（枚举、JSON 对象/数组、强制类型），同时保持底层稳定。核心设计：

- **DO 永不漂移**：DO 与 Mapper.xml 始终使用数据库默认映射的原始类型（枚举列 → Integer/String，json 列 → String），SQL / MyBatis 行为不变；
- **类型变化只发生在 Model 及以上**（领域模型 / DTO / Response / Assembler）；
- **DO ↔ Model 转换全部收敛在仓储 Convertor**，一处生成、全项目生效；
- 查询参数（QueryParam / QueryRequest）保持原始 code 类型，查询一律按数据库存储值。

`columns` 以数据库列名为键：

```yaml
columns:
  user_type: { type: enum, ... }
  tags:      { type: jsonArray, ... }
  extra:     { type: json }
```

未配置的列不写进 `columns`，一律走数据库默认映射（见 7.3.5）。

#### 7.3.1 枚举（type: enum）

```yaml
user_type:
  type: enum
  enum:
    className: UserTypeEnum
    codeType: Integer              # Integer / String / Long；缺省按列类型推断
    values:
      - { code: 0, name: SYSTEM_USER, desc: 系统用户 }
      - { code: 1, name: NORMAL_USER, desc: 普通用户 }
```

生成结果：

- 自动生成枚举类到 `core-model/enums`，实现 `BaseEnum<T>`（T 为 code 类型），带 `@JsonFormat(OBJECT)` 与 `@JsonCreator`；
- DO 保持原始 code 类型（`Integer userType`），Model/DTO 用枚举类型（`UserTypeEnum userType`）；
- Convertor 自动生成 `UserTypeEnum.fromCode(...)` / `ObjectUtil.isNull(...) ? null : ...getCode()` 双向转换；
- 序列化出参为 JSON 对象：`{"code":0,"name":"SYSTEM_USER","desc":"系统用户"}`；入参支持标量 `0` 与对象 `{"code":0}` 两种形态；
- 未匹配的 code 抛业务异常 30004「枚举值未匹配」；
- 查询参数保持原始 code 类型（枚举列查询仍用 Integer/String，原因：MyBatis 默认枚举绑定按 `name()`，查询按 code 最稳）。

校验规则：

- `enum.className` 必填且为合法 Java 类名；
- `codeType` 仅支持 `Integer` / `String` / `Long`，缺省按列推断（int 族 → Integer，bigint → Long，其余 → String）；
- `values` 中 `code` / `name` / `desc` 均必填；`code` 与 `name` 不可重复；`code` 必须能被 `codeType` 解析（Integer 配 `abc` 直接报错）；
- 同名枚举已存在且未配置 `force_create` → **跳过整张表**（不只是枚举文件）。

#### 7.3.2 JSON 数组 / JSON 对象（type: jsonArray / jsonObject）

仅适用于数据库 `json` 类型的列。

```yaml
profile:
  type: jsonObject
  javaObject: com.jakt.aiprod.core.model.domain.Profile   # 目标类型；缺省 Map<String, Object>

tags:
  type: jsonArray
  javaObject: com.jakt.aiprod.core.model.domain.Tag       # 元素类型；缺省 List<Object>
```

- `jsonArray` → Model/DTO 为 `List<javaObject>`（元素类型可用 `String` / `Integer` / POJO 等，泛型也可）；
- `jsonObject` → Model/DTO 为 `javaObject`（POJO 或 `Map<String, Object>`）；
- Convertor 自动用 `JsonUtil.parseArray` / `parseObject` / `parseMap` / `toJson` 互转；
- `jsonObject` + `javaObject: String` 非法（原始字符串请用 `type: json`）；`jsonArray` + `String` 合法（→ `List<String>`）；
- **绑定的 POJO 需自行创建**（如 `Profile` / `Tag`），生成代码的 import 在未创建时标红属预期，创建后即恢复。

#### 7.3.3 原始 JSON（type: json）

```yaml
extra:
  type: json    # 保持数据库 json 列的原始字符串，不解析
```

保持数据库 `json` 列的原始字符串，不解析。不配置 `type` 时默认行为相同（json → String），显式声明仅用于可读性。

#### 7.3.4 强制类型转换（type: 具体 Java 类型）

```yaml
login_count:
  type: Integer        # bigint 列强制转 Integer
```

- 支持 `Integer` / `Long` / `BigDecimal` / `String` / `Double` / `Boolean` / `Float` / `Short` / `Byte` / `Character`；
- 原生类型自动归一化为包装类型（`int` → `Integer`）；
- DO 类型不变（`Long loginCount`），Model/DTO 为指定类型（`Integer loginCount`）；
- 转换统一使用 Hutool `Convert`（`toInt` / `toLong` / `toStr` 等），生成代码禁止裸 `Integer.valueOf`；
- **风险自负**：`bigint → int` 溢出、`varchar → int` 转换失败等由实际数据决定，生成器只保证组合可编译。

#### 7.3.5 默认类型映射（未配置 type）

| 数据库类型 | Java 类型 |
| --- | --- |
| `bigint` | `Long` |
| `int` / `integer` / `smallint` / `mediumint` / `tinyint` | `Integer` |
| `varchar` / `char` / `text` / `longtext` / `mediumtext` / `tinytext` | `String` |
| `datetime` / `timestamp` | `LocalDateTime` |
| `date` | `LocalDate` |
| `decimal` / `numeric` | `BigDecimal` |
| `double` / `float` | `Double` |
| `bit` / `boolean` / `bool` | `Boolean` |
| `json` | `String`（原始 JSON 文本，不自动解析） |

**配置优先级**：列级配置（`type`）> 数据库默认映射。

#### 7.3.6 转换矩阵（Convertor 自动生成）

正向 = DO → Model，反向 = Model → DO：

| DO（默认映射） | Model（目标） | 正向 | 反向 |
| --- | --- | --- | --- |
| 同类型 | 同类型 | 直接赋值 | 直接赋值 |
| Long ↔ Integer | — | `Convert.toInt` | `Convert.toLong` |
| String ↔ 数值/布尔 | — | `Convert.toInt/toLong/toBigDecimal/toDouble/toBool/...` | `Convert.toStr` |
| Integer / String / Long | 枚举 | `XxxEnum.fromCode(...)`（未匹配抛异常） | `ObjectUtil.isNull(...) ? null : getCode()` |
| String（json） | `List<T>` | `JsonUtil.parseArray(x, T.class)` | `JsonUtil.toJson(x)` |
| String（json） | `List<Object>` | `JsonUtil.parseArray(x)` | `JsonUtil.toJson(x)` |
| String（json） | `T`（POJO） | `JsonUtil.parseObject(x, T.class)` | `JsonUtil.toJson(x)` |
| String（json） | `Map<String, Object>` | `JsonUtil.parseMap(x)` | `JsonUtil.toJson(x)` |

#### 7.3.7 生成期校验（fail-fast）

| 场景 | 处理 |
| --- | --- |
| `columns` 键不是表内真实列 | 报错「列 xx 不存在于表 yy」 |
| `type` 不是支持值 | 报错并列出支持值 |
| 普通列配置 `Map` / `Set` / `List` 集合类型 | 报错「集合类型仅用于 json 列」 |
| `type: jsonObject` + `javaObject: String` | 报错，提示改用 `type: json` |
| `type: jsonArray` / `jsonObject` 用于非 json 列 | 报错 |
| 枚举缺 `enum` 块 / `className` 非法 / `code` 或 `name` 重复 / `codeType` 非法 | 分别报错 |
| 转换组合不在支持矩阵内 | 报错「不支持 X→Y 转换」 |
| `javaObject` 非法 | 报错 |

## 8. 生成行为与产物

### 8.1 文件清单

标准表（`generateController: true`）每张表生成 **19 个文件**：

| 模块 | 文件 |
| --- | --- |
| common-dal | `XxxDO`、`XxxMapper`、`XxxMapper.xml` |
| core-model | `Xxx`（领域模型）、`XxxQueryParam` |
| core-repository | `XxxRepository`、`XxxRepositoryImpl`、`XxxConvertor` |
| core-service | `XxxService`、`XxxServiceImpl` |
| biz | `XxxManager`、`XxxManagerImpl` |
| web | `XxxController`、`XxxParamChecker`、`XxxCreateRequest`、`XxxUpdateRequest`、`XxxQueryRequest`、`XxxResponse`、`XxxAssembler` |

另：

- 枚举列自动生成枚举类（`core/model/enums/{EnumName}.java`）；
- 每张表生成 `sql/{db_table_name}.sql`，内容为该表 `SHOW CREATE TABLE` 真实 DDL；
- 内部表（`generateController: false`）只生成上表前 10 个文件（去掉 web 层 7 个与 biz 层 Manager/ManagerImpl 2 个）。

### 8.2 幂等与防覆盖

- 骨架初始化：已存在文件跳过，重复运行安全；
- 表级生成：DO 已存在或同名枚举已存在且未配置 `force_create` → 整表跳过；`force_create: true` → 覆盖全部文件并警告；
- `sql/` 下的 DDL 文件同样遵循跳过 / force 覆盖。

### 8.3 控制台输出与执行报告

- 每张表一行友好消息（成功 / 跳过+原因 / ⚠️ 强制覆盖），不打印逐文件路径噪声；
- 出现跳过或警告时，结束时输出执行报告框（成功 / 跳过 / 警告统计 + 明细）；
- 全部成功只输出一行「全部 N 张表生成成功」。

### 8.4 查询条件与校验注解

- 查询条件：当前全部等值 `=`（含 varchar），另含创建/更新时间区间（`createTimeBegin` / `createTimeEnd` / `updateTimeBegin` / `updateTimeEnd`）与分页；
- 必填校验：NOT NULL 且无默认值的列在 DTO 上生成 `@NotBlank`（字符串）/ `@NotNull`（其他类型）+ varchar `@Size(max)`；
- 非自增主键在 CreateRequest 上自动必填（`@NotBlank` / `@NotNull`），INSERT 显式携带主键列；
- Controller 统一走 `${toolPrefix}Template.execute + Callback`，参数校验在 `beforeService` 调 `${className}ParamChecker`；
- 返回体统一 `${toolPrefix}Result`；日志统一 `${toolPrefix}LoggerUtil`，禁止直接 `LoggerFactory`。

### 8.5 表结构强约束

- **`create_time` / `update_time` 必须存在且由数据库自动维护**：`create_time` 需 `DEFAULT CURRENT_TIMESTAMP`，`update_time` 需 `DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`；生成前强校验，不满足直接报错不给生成（INSERT/UPDATE 不写这两个字段，全部依赖数据库）；
- 必须存在单列主键（按 `PRIMARY KEY` 元数据识别，不假设列名为 `id`）；复合主键报错；
- `create_by` / `update_by` / `del_flag` 为保留审计列，当前不生成 DO 字段；
- 配置的表不存在 → 生成前报错，不落任何半成品文件；
- 本项目不生成单元测试（后期独立测试模块 + 真实数据库）。

## 9. 注意事项

- **创建/更新需传所有 NOT NULL 列**：INSERT / UPDATE 是全列直传，DTO 只强制校验「NOT NULL 且无默认值」的列；漏传带数据库默认值的 NOT NULL 列（如 `level`、`balance`）会触发数据库约束，接口返回 20000「数据不合法：必填字段缺失或违反数据约束」。如需缺省走 DB 默认值，可自行调整模板（INSERT 省略带默认值列）。
- **jsonObject / jsonArray 绑定的 POJO 需自行创建**，生成器不产出（见 7.3.2）。
- LIKE / 范围等复杂查询暂不支持（无法从建表语句推导），后续按需求扩展。
- MySQL 原生 `enum` 列按 `String` 处理；需要枚举语义请配置 `type: enum`（与 int/varchar 枚举完全一致）。
- 逻辑删除是 SQL 层约定：`del_flag` 不生成 DO 字段，手工 SQL 需自行维护该约定。
- 强类型转换（bigint→int 等）自由度由配置人负责，注意溢出与解析失败风险。

## 10. 完整示例

`generate.yaml.example` 内置 3 张示例表，覆盖全部可选配置项：

- `example_inner` → 内部表（`generateController: false`，10 个文件）；
- `example_logic` → 逻辑删除表（仅配置 `db_table_name`，`model_name` / `model_comment` 走缺省；表级 `logicDelete`）；
- `example` → 全功能表（`force_create` + 枚举 ×2 + jsonObject + jsonArray + json + 强制类型转换）。

```yaml
projectPrefix: AiProd
toolPrefix: AiProd
groupId: com.jakt
projectArtifactPrefix: aiprod

jdbc:
  url: jdbc:mysql://localhost:3306/aiprod?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: "123456"

outputDir: /Users/jakt/IdeaProjects/AiProd

tables:
  - db_table_name: example_inner
    model_name: ExampleInner
    model_comment: 内部示例
    generateController: false
    force_create: true

  - db_table_name: example_logic
    logicDelete:
      enable: true
      column_name: del_flag
      normal_value: 0
      delete_value: 1

  - db_table_name: example
    model_name: Example
    model_comment: 示例
    force_create: true
    columns:
      user_type:
        type: enum
        enum:
          className: UserTypeEnum
          codeType: Integer
          values:
            - { code: 0, name: SYSTEM_USER, desc: 系统用户 }
            - { code: 1, name: NORMAL_USER, desc: 普通用户 }
      status:
        type: enum
        enum:
          className: UserStatusEnum
          codeType: String
          values:
            - { code: ENABLED, name: ENABLED, desc: 启用 }
            - { code: DISABLED, name: DISABLED, desc: 停用 }
      login_count:
        type: Integer
      profile:
        type: jsonObject
        javaObject: com.jakt.aiprod.core.model.domain.Profile
      tags:
        type: jsonArray
        javaObject: com.jakt.aiprod.core.model.domain.Tag
      extra:
        type: json
```

## 11. 使用规范与编码军规（CR 2026-08 生效）

生成产物是业务工程代码的一部分，必须遵守项目 AGENTS.md 编码军规。以下为与生成器直接相关的使用要求与已知限制：

| # | 说明 |
|---|---|
| D1 | **状态/类型列一律用生成器枚举配置**（`type: enum`）：Model/QueryParam/DTO 使用枚举，DO/DalQuery 保持数据库原始类型，Convertor 自动做 code↔枚举转换；**枚举 code 必须与数据库存储值一致**（如 `"0"/"1"`），否则运行期不匹配 |
| D2 | **复合主键表已支持**（2026-08 起）：`user_role` / `role_menu` 类关联表可配 `tables`，按全主键生成 ByKey CRUD（Mapper/DO/XML 的 `@Param` 多参 + `pkWhere`）；关联表通常关闭 `generateController`，业务绑定/解绑接口手写 |
| D3 | **敏感列自动剔除**：默认按列名识别 `password/pwd/token/api_key/apikey/secret/salt`，并从 QueryParam/QueryRequest/Response/查询条件中剔除（Model/DO/创建请求保留，登录校验需要）；全局 `sensitive_columns` 可追加自定义列名，列级 `columns.xxx.sensitive: true` 可单独声明 |
| D4 | **时间列不再强校验**：`create_time`/`update_time` 未由数据库自动维护时，生成代码在 INSERT/UPDATE 显式 `NOW()`，不阻塞生成；**`logicDelete` 配置的列必须真实存在**，缺失时生成器输出警告并退化为物理删除（勿静默忽略） |
| D5 | 生成后代码需遵守项目编码军规：嵌套调用拆行、枚举化（查询条件经 `Convertor.toDalQuery` 转原始类型，XML 用 `#{xxx}`）、注释完整（interface 与 private 方法）、Controller 零组装（模板已生成 Assembler 调用）、多写事务用 `BizTemplate.execute(transactionTemplate, ...)` |

## 12. 生成架构（2026-08 重构后）

骨架与模板已对齐 aiplatform 当前架构，生成产物默认满足以下约定：

- **依赖方向**：`common-util` 为最底层（不依赖 core-model）；`core-model` 依赖 `common-util`；`common-dal` / `common-integration` 不依赖 core-model；`web` 不依赖 common-dal / core-repository。
- **基础组件无前缀**：`AssertUtil`（原 AiPlatformInvoker）、`LoggerUtil`、`ConvertUtil`、`ParamValidator`、`ClientInfoUtil`、`Result/PageResult/LogFileEnum` 统一在 `common-util`；web 返回体/模板为 `ApiResult` / `ApiTemplate`（`Callback.beforeService/afterService` 有默认空实现，无参用例无需重写）。
- **查询链路**：`XxxQueryRequest(web) → XxxQueryParam(core-model) → Convertor.toDalQuery → XxxDalQuery(common-dal, extends DalPageQuery) → Mapper/XML`；分页参数 `pageNum/pageSize/offset` 由 `PageQueryRequest → PageParam → DalPageQuery` 逐层传递。
- **仓储返回值**：`insert` 返回回填主键后的 Model；`update/updateByCondition/deleteByXxx` 返回 `int` 受影响行数，0 表示未生效由上层决定；Repository 不抛业务异常（`RESULT_NOT_UNIQUE` 除外）。
- **模板行为**：枚举列 XML 条件直接 `#{status}`（DalQuery 已是原始类型）；QueryParam 用枚举类型 + `modelImports`；Controller 无空 `afterService` 重写；ParamChecker 为 `public final class`，用 `AssertUtil + ParamValidator`；Assembler 方法首行判空、敏感列不进响应。
