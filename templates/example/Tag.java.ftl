package ${basePackage}.core.model.domain;

import lombok.Data;

/**
 * 示例标签 POJO：jsonArray 绑定演示（generateExample 生成，业务可按需替换/删除）。
 */
@Data
public class Tag {

    /** 标签名。 */
    private String name;
}
