<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jakt.aiplatform.common.dal.mapper.UserMapper">

    <!-- 与 sys_user 表一一对应，id/create_time/update_time 由 BaseDO 承载；
         create_by/update_by/del_flag 等审计字段后续由 BizDO 启用，当前不映射 -->
    <sql id="selectColumns">
        id, dept_id, login_name, user_name, user_type, email, phonenumber, sex, avatar, password, salt, status, login_ip, login_date, pwd_update_date, remark, create_time, update_time
    </sql>

    <sql id="queryConditions">
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="deptId != null">
                AND dept_id = #{deptId}
            </if>
            <if test="loginName != null and loginName != ''">
                AND login_name = #{loginName}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name = #{userName}
            </if>
            <if test="userType != null and userType != ''">
                AND user_type = #{userType}
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                AND phonenumber = #{phonenumber}
            </if>
            <if test="sex != null and sex != ''">
                AND sex = #{sex}
            </if>
            <if test="avatar != null and avatar != ''">
                AND avatar = #{avatar}
            </if>
            <if test="password != null and password != ''">
                AND password = #{password}
            </if>
            <if test="salt != null and salt != ''">
                AND salt = #{salt}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="loginIp != null and loginIp != ''">
                AND login_ip = #{loginIp}
            </if>
            <if test="loginDate != null">
                AND login_date = #{loginDate}
            </if>
            <if test="pwdUpdateDate != null">
                AND pwd_update_date = #{pwdUpdateDate}
            </if>
            <if test="remark != null and remark != ''">
                AND remark = #{remark}
            </if>
            <if test="createTime != null">
                AND create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                AND update_time = #{updateTime}
            </if>
        </where>
    </sql>

    <select id="selectById" resultType="com.jakt.aiplatform.common.dal.dataobject.UserDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="com.jakt.aiplatform.common.dal.dataobject.UserDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_user
        <include refid="queryConditions"/>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectList" resultType="com.jakt.aiplatform.common.dal.dataobject.UserDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_user
        <include refid="queryConditions"/>
        ORDER BY id DESC
    </select>

    <select id="countByQuery" resultType="long">
        SELECT COUNT(*)
        FROM sys_user
        <include refid="queryConditions"/>
    </select>

    <insert id="insert" parameterType="com.jakt.aiplatform.common.dal.dataobject.UserDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (dept_id, login_name, user_name, user_type, email, phonenumber, sex, avatar, password, salt, status, login_ip, login_date, pwd_update_date, remark, create_time, update_time)
        VALUES (#{deptId}, #{loginName}, #{userName}, #{userType}, #{email}, #{phonenumber}, #{sex}, #{avatar}, #{password}, #{salt}, #{status}, #{loginIp}, #{loginDate}, #{pwdUpdateDate}, #{remark}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.jakt.aiplatform.common.dal.dataobject.UserDO">
        UPDATE sys_user
        SET dept_id = #{deptId},
            login_name = #{loginName},
            user_name = #{userName},
            user_type = #{userType},
            email = #{email},
            phonenumber = #{phonenumber},
            sex = #{sex},
            avatar = #{avatar},
            password = #{password},
            salt = #{salt},
            status = #{status},
            login_ip = #{loginIp},
            login_date = #{loginDate},
            pwd_update_date = #{pwdUpdateDate},
            remark = #{remark},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>
</mapper>
