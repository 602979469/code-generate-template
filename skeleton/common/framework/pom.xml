<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.jakt</groupId>
        <artifactId>aiplatform-common</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>aiplatform-common-framework</artifactId>
    <name>aiplatform-common-framework</name>
    <description>共享框架层：Result/PageResult、异常体系（ErrorCode/CommonException/ErrorCodeEnum）、AssertUtil/LoggerUtil/ParamValidator、事务模板、BaseModel/PageParam 等公共类；CRUD 生成直接依赖</description>

    <dependencies>
        <!-- slf4j：LoggerUtil / TraceIdUtil（MDC） -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <!-- Hutool：AssertUtil / ParamValidator 判空与校验 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <!-- jakarta validation：ParamValidator 使用 -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
    </dependencies>
</project>
