# 场景：tables 中重复配置 example（预判 R3：文档要求报错，实现可能静默）
generateExample: true
projectPrefix: DupTest
toolPrefix: DupTest
groupId: com.jakt
projectArtifactPrefix: duptest

jdbc:
  url: jdbc:mysql://localhost:3306/aitest?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: "123456"

outputDir: /tmp/aitest-tests/exampledup

tables:
  - db_table_name: example
    model_name: Example
    model_comment: 示例
