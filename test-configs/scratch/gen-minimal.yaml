# 场景：v1 最小配置回归（命名 + jdbc + tables 三件套，无任何新选项）
projectPrefix: MiniTest
toolPrefix: MiniTest
groupId: com.jakt
projectArtifactPrefix: minitest

jdbc:
  url: jdbc:mysql://localhost:3306/aitest?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
  username: root
  password: "123456"

outputDir: /tmp/aitest-tests/minimal

tables:
  - db_table_name: sys_dept
    model_name: Dept
    model_comment: 部门
